import { SafeAreaView, Text } from 'react-native';

export default function SharedWishListScreen() {
    return (
        <SafeAreaView>
            <Text>Shared Wish List</Text>
        </SafeAreaView>
    );
}